<%= form_for(@pet_club) do |f| %>
  <% if @pet_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pet_club.errors.count, "error") %> prohibited this pet_club from being saved:</h2>

      <ul>
      <% @pet_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
